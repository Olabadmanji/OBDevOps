# =============================================================
# Terraform State Files
# terraform.tfstate is auto-generated when you run terraform apply.
# It stores the current state of your AWS infrastructure including
# resource IDs, IP addresses, and sometimes secret values in plaintext.
# Committing this exposes your entire AWS infrastructure details.
# =============================================================
*.tfstate
*.tfstate.backup
.terraform.tfstate.lock.info

# =============================================================
# Terraform Variables File
# terraform.tfvars contains your actual secret values like
# passwords that you pass into Terraform at runtime.
# This file should NEVER be on GitHub.
# =============================================================
terraform.tfvars
*.auto.tfvars

# =============================================================
# Terraform Provider Binaries
# The .terraform folder is created by terraform init and contains
# downloaded AWS provider binaries (hundreds of MBs).
# It is not your code — anyone can regenerate it with terraform init.
# =============================================================
.terraform/
.terraform.lock.hcl

# =============================================================
# Environment Files
# These may contain API keys, database URLs, and secrets.
# =============================================================
.env
.env.local
.env.production

# =============================================================
# Application Dependencies
# node_modules is created by npm install.
# It is not your code — anyone can regenerate it with npm install.
# =============================================================
app/node_modules/
npm-debug.log

# =============================================================
# AWS Credentials and Keys
# Should NEVER be committed under any circumstances.
# =============================================================
.aws/
*.pem
*.key
credentials

# =============================================================
# OS and Editor Clutter
# These are generated by your operating system and VS Code.
# They have nothing to do with your project.
# =============================================================
.DS_Store
Thumbs.db
.vscode/settings.json